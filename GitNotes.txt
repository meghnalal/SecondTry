Create Repo ---->

                git init
                git add .
                git status 
                git commit -m "comments"
                git remote add origin https://github.com/meghnalal/First_Project.git
                git push -u origin main
Create a Branch --->
                git branch
                git checkout -b mynewbranch   
Push a Branch   --->
                git push  <REMOTENAME> <BRANCHNAME> 
                git push origin newbranch
Pull Request   --->